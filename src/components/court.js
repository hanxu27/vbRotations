import styled from "styled-components";
import image from "./volleyball_court.jpg";

const background = styled.div`
  background-image: url(${image});
  background-size: cover;
  background-position: center;
`;

export default background;
